%h1
  New entry
  = @blog.title
= link_to 'Back', :back

= simple_form_for([@blog, @entry], url: { action: :create }) do |f|
  = f.input :title, label: "Title"
  = f.input :body, label: "Body"
  = f.button :submit, value: "Save"
